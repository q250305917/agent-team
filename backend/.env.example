# 后端环境变量配置示例
# 复制此文件为 .env 后进行配置

# WebSocket 认证令牌
# 生产环境建议使用更复杂的随机字符串
WS_TOKEN=agent-teams-dashboard-secure-token

# CORS 允许的源（多个用逗号分隔）
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 服务器配置
HOST=0.0.0.0
PORT=8000
